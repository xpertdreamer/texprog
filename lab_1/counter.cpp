#include "counter.h"
#include <string>

bool Counter::tokenize() {
  DEBUG("Call tokenize\n");
  std::size_t i = 0;
  std::string token;
  const std::string delimiters = ",.;!?: \t\n\r";

  // TODO: tokenize with complexity O(n)

  return true;
}
